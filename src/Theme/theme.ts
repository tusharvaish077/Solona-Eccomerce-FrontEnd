import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode:"light",
    primary: {
      main: '#00927c', // Your brand orange
    },
    secondary: {
      main: '#EAF0F1',
    },
  },
});

export default theme;
